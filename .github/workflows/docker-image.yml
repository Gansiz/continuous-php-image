name: Docker Image CD

on:
  push:
    branches: [ "master" ]

jobs:

  build:

    runs-on: ubuntu-latest

    steps:
    - name: Checkout the codebase
      uses: actions/checkout@v4
    
    - name: Build the Docker image
      run: docker build . -t gansizz/continuous_php_image
